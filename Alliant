Create the MSSQL target file. CyberArk's database discovery uses targeted CSV-based scanning. The CSV tells Discovery which database targets to scan and their access details. CyberArk introduced this specifically for MSSQL, Oracle, MySQL, and PostgreSQL.

We can use the current Reconcile account to scan since it has permission to do so.

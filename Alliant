If you only know the MSSQL target/FQDN and port, that is enough to identify where SQL Server is listening, but it is not enough for CyberArk to perform an authenticated database discovery. The Management Agent still needs some credential with which to connect to SQL Server and enumerate the SQL principals. CyberArk describes the database scan as a targeted scan whose target data is used to obtain an appropriate vaulted credential at runtime.

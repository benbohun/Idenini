how do i add this account to these policies if its controlled at GPO level, since cyberark mention its part of pre-requisite

Required Rights for NT SERVICE\CyberArkDPAConnector

CyberArk specifically calls out the following requirements when the connector service fails to start:

Required Privileges

  *   SeChangeNotifyPrivilege (Bypass traverse checking)
  *   SeCreateGlobalPrivilege (Create global objects)
  *   SeIncreaseWorkingSetPrivilege (Increase a process working set)
Required User Right

  *   SeServiceLogonRight (Log on as a service)

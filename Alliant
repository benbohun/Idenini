Multiple users are seeing authentication error from User Portal when attempting to Single Signon via Okta.
Attached is what the experience looks like

When users attempt to switch authentication using Okta FastPass, they are getting the above error

User portal is prompting a mandatory registration before creating or using a passkey

Is there a way to stop this prompt so it doesnt conflict the Okta biometric login auth method

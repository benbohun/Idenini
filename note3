cat /etc/passwd — enumerate local users (login name, UID/GID, home, shell). 
CyberArk Documentation
+1

cat /etc/shadow — password/lock/expiration info (needs sudo/root). 
CyberArk Documentation
+1

cat /etc/group — group memberships. 
CyberArk Documentation

cat /etc/sudoers (and included files) — detect sudo privileges. 
CyberArk Documentation

lastlog | grep -v '*' — last-login time per account. 
CyberArk Documentation

hostname -s — capture short hostname used as the discovered “address.”
